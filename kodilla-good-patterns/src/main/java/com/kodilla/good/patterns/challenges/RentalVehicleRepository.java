package com.kodilla.good.patterns.challenges;

public class RentalVehicleRepository implements RentalRepository {

    @Override
    public boolean createRental(User user, Vehicles vehicles) {
        return true;
    }
}
